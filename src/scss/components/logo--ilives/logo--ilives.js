
/*
Drupal.behaviors.logo--ilives = {
  attach: function (context, settings) {
    $('#some_element', context).once('logo--ilives', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
