
/*
Drupal.behaviors.showcase = {
  attach: function (context, settings) {
    $('#some_element', context).once('showcase', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
