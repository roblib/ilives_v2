
/*
Drupal.behaviors.block--lp-site-description = {
  attach: function (context, settings) {
    $('#some_element', context).once('block--lp-site-description', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
