
/*
Drupal.behaviors.adv-search = {
  attach: function (context, settings) {
    $('#some_element', context).once('adv-search', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
