
/*
Drupal.behaviors.search-result = {
  attach: function (context, settings) {
    $('#some_element', context).once('search-result', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
