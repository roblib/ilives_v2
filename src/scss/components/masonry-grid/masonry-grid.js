
$('.book-grid').masonry({
    itemSelector: '.book-grid > div'
});
/*
Drupal.behaviors.masonry-grid = {
  attach: function (context, settings) {
    $('#some_element', context).once('masonry-grid', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
