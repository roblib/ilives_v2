
/*
Drupal.behaviors.navbar--menu = {
  attach: function (context, settings) {
    $('#some_element', context).once('navbar--menu', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
