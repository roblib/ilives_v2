
/*
Drupal.behaviors.facet-pages = {
  attach: function (context, settings) {
    $('#some_element', context).once('facet-pages', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
