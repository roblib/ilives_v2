
/*
Drupal.behaviors.navbar--search = {
  attach: function (context, settings) {
    $('#some_element', context).once('navbar--search', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
