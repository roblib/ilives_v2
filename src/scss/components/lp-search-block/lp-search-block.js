
/*
Drupal.behaviors.lp-search-block = {
  attach: function (context, settings) {
    $('#some_element', context).once('lp-search-block', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
