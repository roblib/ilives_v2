
/*
Drupal.behaviors.block--lp-feature = {
  attach: function (context, settings) {
    $('#some_element', context).once('block--lp-feature', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
