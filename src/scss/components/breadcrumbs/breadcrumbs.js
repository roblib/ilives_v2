
/*
Drupal.behaviors.breadcrumbs = {
  attach: function (context, settings) {
    $('#some_element', context).once('breadcrumbs', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
