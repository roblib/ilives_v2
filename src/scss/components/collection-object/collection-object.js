
/*
Drupal.behaviors.collection-object = {
  attach: function (context, settings) {
    $('#some_element', context).once('collection-object', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
