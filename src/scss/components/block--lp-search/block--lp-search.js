
/*
Drupal.behaviors.block--lp-search = {
  attach: function (context, settings) {
    $('#some_element', context).once('block--lp-search', function () {
      // Code here will only be applied to $('#some_element')
      // a single time.
    });
  }
};
*/
